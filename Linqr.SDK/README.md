# Linqr.SDK

This Software Development Kit (SDK) contains all the core domain logic for Linqr.

Spectre libraries are reserved as implementation details in this project, not dependencies. The core
domain architecture should be independent of any specific presentation implementations and should be
interfaced through request pipelines.